---
- name: check the diff between old and new ceph.conf
  command: "diff --changed-group-format='%>' --unchanged-group-format='' /etc/ceph/{{ cluster }}.conf /tmp/{{ cluster }}.conf"
  register: diffcephconf

- name: inject the configuration option
  command: 

- name: verify that the configuration option was correctly injected
  command: ceph daemon 

