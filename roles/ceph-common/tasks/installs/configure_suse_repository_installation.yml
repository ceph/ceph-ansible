---
- name: include suse_obs_repository.yml
  include_tasks: suse_obs_repository.yml
  when:
    - ceph_repository == 'obs'

- name: include suse_custom_repository.yml
  include_tasks: suse_custom_repository.yml
  when:
    - ceph_repository == 'custom'
