---
- name: container registry authentication
  command: '{{ container_binary }} login -u {{ ceph_container_registry_username }} -p {{ ceph_container_registry_password }} {{ ceph_container_registry }}'
  changed_when: false
  no_log: true
